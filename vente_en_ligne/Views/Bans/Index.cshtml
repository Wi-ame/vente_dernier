@model IEnumerable<vente_en_ligne.Models.Ban>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IDP)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IDP)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id_Ban">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id_Ban">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id_Ban">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
