@model vente_en_ligne.Models.Utilisateur

@{
    ViewData["Title"] = "Create";
}

<link type="text/css" rel="stylesheet" href="/css/CreateU.css" />
<div class="alert alert-danger" style="background: rgba(0,0,0,.5);font-family: 'Berlin Sans FB';color:r#D41818;
;display: @(!string.IsNullOrEmpty(ViewData["ErrorMessage"] as string) ? "block" : "none");" id="errorMessage">
    @ViewData["ErrorMessage"]
</div>
<div class="login-box">
    <h2 style="color:#D41818;font-family: 'Berlin Sans FB';">Become Client</h2>

    <form asp-action="Authent" method="post">
        <div class="user-box">

            <label style="color:white;font-family: 'Berlin Sans FB';"> Email</label>
            <br>
            <br>
            <input asp-for="Email" class="form-control" required />

            <div class="align">
            </div>
            <button type="submit" style="background:#D41818;font-family: 'Berlin Sans FB'; color:white;margin-bottom:25px; ">
                Submit
            </button>

            <button style="background:#D41818; color:white;font-family: 'Berlin Sans FB';margin-left:140px; margin-bottom:20px;">
                <a href="@Url.Action("Create", "Utilisateurs")" style="color:white ;">Create User</a>
            </button>
        </div>
    </form>

</div>